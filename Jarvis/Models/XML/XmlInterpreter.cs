using System.IO;

namespace Jarvis
{
    class XmlInterpreter
    {
        public XmlInterpreter(string _path)
        {
            StreamReader reader = new StreamReader(_path);
            
        }
    }
}
